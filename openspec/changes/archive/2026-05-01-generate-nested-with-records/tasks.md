## 1. Разведка текущей генерации `With_`

- [x] 1.1 Найти текущую точку входа генератора `ExtendedWith` и место, где формируется список свойств для генерации `With_` методов
- [x] 1.2 Зафиксировать текущие правила включения свойств (public/instance, init/setter, nullable) и текущий шаблон имени `With_<Property>`
- [x] 1.3 Добавить/обновить тестовый fixture, который демонстрирует текущую генерацию “верхних” `With_` методов (для регрессии)

## 2. Анализ record-графа и построение путей

- [x] 2.1 Реализовать определение “record-typed property” через Roslyn (`INamedTypeSymbol.IsRecord`, unwrap nullable)
- [x] 2.2 Реализовать обход record-свойств в глубину с накоплением “пути” сегментов и набором `visited` для защиты от циклов
- [x] 2.3 Реализовать детекцию nullable record-сегментов по пути и пометку путей как “nullable-path”
- [x] 2.4 Реализовать исключение свойств с атрибутом `NoNestedWith` из построения путей
- [x] 2.5 Реализовать выбор leaf-свойств (генерировать методы только для leaf, который не является record)

## 3. Генерация путевых методов `With_<path>` (единый конвейер)

- [x] 3.1 Сформировать стабильное имя метода `With_` + сегменты пути через `_` (например `With_Address_City`); односегментные пути — тот же конвейер, что и многосегментные
- [x] 3.2 Сгенерировать тело метода, которое создаёт новый экземпляр исходного типа и обновляет значения по пути через `with`-копирование record на каждом record-сегменте
- [x] 3.3 Для каждого базового имени (`With_<path>`, при необходимости `TryWith_<path>` / `With_<path>_OrThrow`) сгенерировать **восемь** перегрузок: значение, `Task<>`, синхронный и асинхронный `Func` × приёмник `this Root` / `this Task<Root>`
- [x] 3.4 Для “nullable-path” сгенерировать семейство `With_<path>_OrThrow` (8 перегрузок) с **явным** `throw new NullReferenceException(...)` и информативным сообщением при `null` на nullable record-сегменте
- [x] 3.5 Для “nullable-path” сгенерировать семейство `TryWith_<path>` (8 перегрузок) с семантикой null propagation (при `null` по пути — вернуть `source` без изменений)
- [x] 3.6 Добавить строгую обработку коллизий имени метода из-за неоднозначного `PathSuffix`: если разные допустимые пути порождают одно и то же базовое имя семейства (`With_...` / `TryWith_...` / `With_..._OrThrow`), система MUST бросить исключение и прекратить генерацию для этого record-типа (сигнатуры/типы не учитываются)
- [x] 3.6.1 Реализовать детекцию коллизий строго по базовому имени метода (из `PathSuffix`), сохраняя как минимум два неоднозначных пути/их описания для сообщения об ошибке
- [x] 3.6.2 Убрать/отключить текущую дедупликацию по сигнатурам (когда разные пути производят одинаковые `With_...` имена, но C# мог бы разрешить перегрузки по типам)
- [x] 3.7 Убедиться, что публичная форма “верхних” `With_<Property>` (8 перегрузок, семантика) сохраняется (регрессия)

## 4. Тесты на требования и edge-cases

- [x] 4.1 Добавить тест: генерация `With_Address_City` для 1 уровня вложенности record
- [x] 4.2 Добавить тест: генерация `With_A_B_X` для нескольких уровней вложенности record
- [x] 4.2.1 Добавить тест: при наличии `With_A_B_X` также присутствуют `With_A` и `With_A_B`
- [x] 4.3 Добавить тест: лямбда-перегрузка `Func<LeafType, LeafType>` генерируется и использует текущее leaf-значение
- [x] 4.4 Добавить тест: nullable record-сегмент (`SomeRecord?`) → генерируются `TryWith_<path>` (null propagation) и `With_<path>_OrThrow` (throw)
- [x] 4.5 Добавить тест: null propagation — при `null` по пути возвращается исходный `source` без изменений
- [x] 4.6 Добавить тест: throw — при `null` по пути бросается NRE
- [x] 4.7 Добавить тест: цикл типов в record-графе → генерация не уходит в бесконечность и не создаёт “циклические” пути
- [x] 4.8 Добавить/обновить тест: коллизия имён из-за неоднозначного `PathSuffix` (например кейс `OuterCollision` с `A.B` и `A_B`) → генерация завершается ошибкой (исключением), а не дедупликацией сигнатур
- [x] 4.11 Обновить тест `CollidingGeneratedMethodNames_AreDeduped` (или заменить) так, чтобы он валидировал ожидаемую ошибку генератора при конфликте базового имени метода
- [x] 4.9 Добавить тест: `NoNestedWith` на record-сегменте → вложенные методы `With_A_...`/`TryWith_A_...`/`With_A_..._OrThrow` не генерируются, но “верхний” `With_A` остаётся
- [x] 4.10 Добавить тест: `NoNestedWith` на leaf → соответствующий метод не генерируется

## 4b. Исправление: null guard не должен применяться к цели пути

- [x] 4b.1 Убрать форсированный `true` для func-перегрузок в `LoadRecordPrefixWithNullChecks` — оба семейства (value и func) должны использовать один и тот же `targetIsLeaf`
- [x] 4b.2 Упростить `IsAssignmentTarget` в `LoadRecordPrefixWithNullChecks`: заменить `!targetIsLeaf && i == path.Count - 1` на `i == path.Count - 1` (параметр `targetIsLeaf` в методе стал эквивалентен)
- [x] 4b.3 Исправить тест `TryWith_FuncOverload_NullableRecordTarget_WhenNull_*` — старое ожидание (func не вызывается) было неверным; новое ожидание: func вызывается с `null`, результат применяется
- [x] 4b.4 Добавить тест `OrThrow_FuncOverload_NullableRecordTarget_WhenNull_*`: исключение не должно бросаться при null-цели
- [x] 4b.5 Добавить тест: traversal-null по-прежнему блокирует вызов func (регрессия на правило traversal-only)
- [x] 4b.6 Обновить design.md (раздел 5): явно зафиксировать, что null guard применяется только к traversal-узлам, но не к цели
- [x] 4b.7 Обновить spec.md: добавить сценарий "Null guard не применяется к цели пути"

## 5. Шлифовка и документация для пользователя

- [x] 5.1 Добавить минимальные примеры (в тестах/fixtures) использования вложенных `With_` методов, чтобы было видно ожидаемое API
- [x] 5.2 Проверить, что сгенерированный код компилируется для типовых комбинаций `record` (positional record, record class) и что форматирование соответствует стилю проекта
- [x] 5.3 Обновить публичную документацию в `Readme.md`: описать вложенные методы, `TryWith_<path>`/`With_<path>_OrThrow` и атрибут `NoNestedWith`
